
학번 :

이름 :

  
  
  
  

#### 1. Java 프로그램의 5가지 실행 단계를 올바른 순서로 나열한 것은?

1. edit \> load \> compile \> verify \> execute

2. edit \> compile \> load \> verify \> execute

3. compile \> edit \> verify \> load \> execute

4. edit \> compile \> verify \> load \> execute

  

#### 2. Java Compiler가 소스코드를 컴파일하면 생성되는 것은?

1. 기계어 파일

2. 어셈블리 파일

3. 바이트 코드가 담긴 `.class` 파일

4. 실행가능한 `.exe` 파일

  

#### 3. 다음 중 객체지향 프로그래밍의 캡슐화(Encapsulation)에 대한 설명으로 올바른 것은?

1. 기존 클래스를 확장하여 새로운 클래스를 만드는 것

2. 속성과 메서드를 객체 안에 감싸고, 구현 세부 사항을 숨기는 것

3. 하나의 클래스로부터 여러 객체를 생성하는 것

4. 프로그램을 여러 개의 함수로 나누는 것

  

#### 4. 다음 중 Java의 % 연산자에 대한 설명으로 올바른 것은?

1. 백분율을 계산

2. 나눗셈의 몫을 반환

3. 나눗셈의 나머지를 반환

4. printf에서만 사용되는 특수 기호

  

#### 5. 다음 코드의 출력 결과는?

```java

int x = 10, y = 3;

if(x % y == 1)

    System.out.printf("x = %d, y=%d\n", x, y);

if(x != y)

    System.out.printf("not equal");

```

  

##### 출력

```

  
  

```

  

#### 6. Java가 플랫폼 독립적인 이유는?

```

  
  
  

```

  

#### 7. 다음 수식의 Java 연산 결과를 연산자 우선순위에 따라 단계별로 나타내시오.

```

2 + 3 * 4 - 8 / 2

```